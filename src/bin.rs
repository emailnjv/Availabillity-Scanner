use dmv_api;

use std::collections::HashMap;
use std::fs::File;
use std::io::Read;
use serde_json::Value;
use serde;
use crate::lib::structs::LocationMap;


#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}

